# Koole 物理模拟器 — Claude 项目指令

## 🔴 禁止：git 操作和 PowerShell Edit 功能

**本项目内禁止使用以下操作**（之前出过乱子）：

- ❌ **禁止 `git commit` / `git push` / `git pull` / `git rebase` 等写操作**——用户可以自己在终端里操作 git，AI 不要碰
- ❌ **禁止用 PowerShell 的 `Set-Content` / `Out-File` / `Write-Output` 编辑已有文件**——特别是 UTF-16 LE 编码问题会炸掉 UTF-8 文件
- ✅ 文件编辑统一用 Read → Edit / Write 工具

---

## 项目概述

初中/高中物理交互式模拟器，纯前端架构（Vue 3），无后端依赖。所有模型数据在 `Koole-frontend/src/constants/models/*.js`，渲染逻辑在 `modelRenderers.js`，物理参数在 `modelPhysics.js`，模型注册在 `physicsModels.js`。

---

## 一、模型数据文件格式（简单易懂的知识内容）

所有 `models/*.js` 必须严格遵循以下格式。`knowledge` 字段用于展示准确、易懂、有一点个人风格的物理知识内容。不要写成死板教材，也不要只写碎片化感想。

### 格式速查：knowledge 字段

```
## 模型名

[概念定义——说明研究对象、适用条件和核心现象]

[核心规律与公式——$$ 块级 $$ 或 - $ 列表 $]

其中：[变量说明，用中文逗号、分号，$变量$ 包裹]

[物理意义——解释公式背后的含义、变量之间的关系、适用范围]

[推导思路或规律分析——必要时给出简洁推导，不写得过散]

[典型应用——结合常见情境说明该规律如何使用]

> [参数观察——说明调节哪些参数会看到什么变化]
```

**格式硬性规则**：
- 段落之间必须有**空行**
- "其中："后跟**中文冒号**（`：`），不是英文冒号
- 公式用 `$...$`（行内）或 `$$...$$`（块级）或 `- $...$` 列表
- 参数观察以 `>` 开头，格式 `> 内容`（`>` 后有一空格）
- 变量说明中的逗号、分号用**中文标点**

**内容检查清单**（写完后逐条检查）：
- [ ] 概念、公式、单位和符号是否准确？
- [ ] 是否写清楚适用条件和物理意义？
- [ ] 是否避免只有口号式结论，必要处有解释或推导？
- [ ] 公式和变量说明是否使用中文标点？
- [ ] 段落之间是否有空行？

### 1.2 参考模式速查

| 部分 | 格式 | 示例 |
|------|------|------|
| 开头 | `## 模型名` + 空行 + 概念定义 | `## 自由落体` → 空行 → `自由落体是物体只受重力作用、初速度为零的竖直运动。` |
| 公式 | `- $...$` 列表或 `$$...$$` | `- $v = gt$` |
| 变量说明 | `其中：` | `其中：$v$ 是末速度，$g$ 是重力加速度。` |
| 物理意义 | 自然段 | `速度与时间成正比，位移与时间的平方成正比。` |
| 推导分析 | 自然段或公式块 | `由 $a=g$ 和 $v_0=0$ 可得 $v=gt$。` |
| 典型应用 | 自然段 | `竖直下落的小球、忽略空气阻力的抛体下落段都可近似使用。` |
| 参数观察 | `> 内容` | `> 增大 $g$ 后，速度增长更快，到达地面的时间更短。` |

---

## 二、中文语言规范

### 2.1 标点符号
- 中文逗号 `，` / 中文句号 `。` / 中文冒号 `：` / 中文分号 `；` / 中文问号 `？`
- 英文括号 `()` 包裹变量（$变量$ 本身已区分，无需括号）
- 引号统一用 `""`
- 破折号用 `——`（两个 em dash）

### 2.2 语气和风格
- 使用简单、清楚、准确的物理说明
- 可以有一点个人风格，比如"这个地方容易看错"、"这里抓住一个关系就行"，但不要太散
- 可以解释概念、推导公式、说明适用条件和典型应用
- 避免写成死板教材，也避免只写个人感受
- 内容要能直接作为模型页面的知识说明使用

### 2.3 技术术语
- 变量名保留英文（Canvas, Math.asin, devicePixelRatio）
- 物理术语用中文全称（折射率、全反射、临界角）
- 人名用中文译名（伽利略、阿基米德、胡克、斯涅尔）

---

## 三、Canvas 渲染约定

所有 renderer 在 `modelRenderers.js` 中，遵循以下约定：

### 3.1 HiDPI 适配
```js
const dpr = window.devicePixelRatio || 1
ctx.scale(dpr, dpr)
```

### 3.2 颜色主题感知
```js
const isDark = theme === 'dark'
const textColor = isDark ? '#e0e0e0' : '#333'
const bgColor = isDark ? '#1a1a2e' : '#fff'
```

### 3.3 绘制层级
1. 背景 / 水面 / 地面
2. 光线 / 物体
3. 标注 / 标签
4. 刻度 / 辅助线

### 3.4 常见模式
- 光线透明度递增/递减效果：`globalAlpha`
- 虚像用虚线：`ctx.setLineDash([5, 5])`
- 光源发光效果：外层 glow + 内层 core
- 角度标注：`ctx.arc()` + 文字

---

## 四、代码风格

- ES modules（`export default`）
- 无 TypeScript
- 注释用中文
- 变量命名用驼峰（英文）
- 渲染相关函数在 `modelRenderers.js`，物理逻辑在 `modelPhysics.js`

---

## 五、Git 工作流

- 主分支：`main`
- 当前工作分支：`pure-frontend`
- 无后端依赖，所有数据在 constants 目录
