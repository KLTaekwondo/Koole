# Koole 物理模拟器 — Claude 项目指令

## 🔴 最高优先级：ICP 备案红线（仅 Koole-frontend/ 目录）

**这个网站（Koole-frontend/）要过中国 ICP 备案。个人网站不能有任何教育/教学特征，否则直接打回。**

仅 `Koole-frontend/` 下的文件受 ICP 约束，项目根目录下的工具文件不受限。

**所有显示给用户的文字（`knowledge` 字段）必须全是个人笔记风格**——就像学生自己写的复习笔记，不是老师在讲课。**绝对禁止**以下任何字眼和语气：

所有显示给用户的文字（`knowledge` 字段）**必须**是**个人笔记风格**——就像学生自己写的复习笔记，不是老师在讲课。**绝对禁止**以下任何字眼和语气：

| ❌ 禁止使用 | ✅ 换成 |
|------------|--------|
| 本实验 / 本模型演示 | （直接说现象，不加前缀） |
| 请思考 / 想一想 | （不用说教指令） |
| 同学们 / 大家 | （没人在听课） |
| 知识点 / 考点 / 必考 | （这是笔记，不是教辅） |
| 掌握 / 理解 / 学会 | （个人笔记不说这些） |
| 总结一下 / 归纳如下 | （直接写内容） |
| 你 / 你们 | （改成"我"或直接陈述） |
| 应 / 应当 / 应该 | （改成自然描述） |

**凡是写成"教育"的，ICP 审核必死。这条没商量。**

---

## 🔴 禁止：git 操作和 PowerShell Edit 功能

**本项目内禁止使用以下操作**（之前出过乱子）：

- ❌ **禁止 `git commit` / `git push` / `git pull` / `git rebase` 等写操作**——用户可以自己在终端里操作 git，AI 不要碰
- ❌ **禁止用 PowerShell 的 `Set-Content` / `Out-File` / `Write-Output` 编辑已有文件**——特别是 UTF-16 LE 编码问题会炸掉 UTF-8 文件
- ✅ 文件编辑统一用 Read → Edit / Write 工具

---

## 项目概述

初中/高中物理交互式模拟器，纯前端架构（Vue 3），无后端依赖。所有模型数据在 `Koole-frontend/src/constants/models/*.js`，渲染逻辑在 `modelRenderers.js`，物理参数在 `modelPhysics.js`，模型注册在 `physicsModels.js`。

---

## 一、模型数据文件格式（必须是个人笔记风格）

所有 `models/*.js` 必须严格遵循以下格式。**参考基准文件：`free-fall.js`**。

### 格式速查：knowledge 字段

```
## 模型名

[一句话开头——极简，破折号或直述，像自己小声嘀咕]

[公式——$$ 块级 $$ 或 - $ 列表 $]

其中：[变量说明，用中文逗号分号，$变量$ 包裹]

[个人风格笔记——口语化、带个人观点，如"这个很多人搞错"、"很好记"、"选择题很爱考"]

[生活/实际例子——简短、个人经验感]

> [简短提示——呼唤用户操作]
```

**格式硬性规则**：
- 段落之间必须有**空行**
- "其中："后跟**中文冒号**（`：`），不是英文冒号
- 公式用 `$...$`（行内）或 `$$...$$`（块级）或 `- $...$` 列表
- tip 以 `>` 开头，格式 `> 内容`（> 后有一空格）
- 变量说明中的逗号、分号用**中文标点**

**语气检测清单**（写完后逐条检查）：
- [ ] 读起来像一个人在自己记笔记，而不是在给别人讲课？
- [ ] 没有"本实验"、"请思考"、"同学们"、"掌握"这些词？
- [ ] 公式和变量说明用中文标点？
- [ ] 段落之间有空行？

### 格式速查：devNotes 字段

```
## 开发笔记

[技术背景/模型对比——如"XX跟YY的区别是..."]

[代码块，用 ```js 包裹]

[渲染/实现细节——Canvas 绘制要点、踩过的坑]

[参数交互说明——调什么参数能看到什么效果]
```

**格式硬性规则**：
- 必须有 `## 开发笔记` 标题
- 代码块用三重反引号包裹，标注 `js`
- 段落之间有空行

**语气**：技术向但随意，像是在跟同行聊天，可以吐槽踩坑。

### 1.3 参考模式速查

| 部分 | 格式 | 示例 |
|------|------|------|
| 开头 | `## 模型名` + 空行 + 一句话 | `## 自由落体` → 空行 → `匀变速直线运动的特例——只受重力...` |
| 公式 | `- $...$` 列表或 `$$...$$` | `- $v = gt$` |
| 变量说明 | `其中：` | `其中：$v$ 是末速度...` |
| 个人笔记 | 自然段 | `这个很多人搞错...` 或 `有个结论做题经常用...` |
| 例子 | 自然段 | `伽利略比萨斜塔实验说的就是这个` |
| 提示 | `> 内容` | `> 把 g 调成 1.6（月球）...` |

---

## 二、中文语言规范

### 2.1 标点符号
- 中文逗号 `，` / 中文句号 `。` / 中文冒号 `：` / 中文分号 `；` / 中文问号 `？`
- 英文括号 `()` 包裹变量（$变量$ 本身已区分，无需括号）
- 引号统一用 `""`
- 破折号用 `——`（两个 em dash）

### 2.2 语气和风格
- **个人笔记体**，不要教育/教学语气
- 避免词汇：`本实验` `请思考` `同学们` `知识点` `掌握` `总结一下` `应/应当/应该`
- 偏好词汇：`这个很容易搞错` `做题经常遇到` `重点关注` `挺有意思的` `老忘记这个`
- 用生活例子类比，不说教

### 2.3 技术术语
- 变量名保留英文（Canvas, Math.asin, devicePixelRatio）
- 物理术语用中文全称（折射率、全反射、临界角）
- 人名用中文译名（伽利略、阿基米德、胡克、斯涅尔）

---

## 三、Canvas 渲染约定

所有 renderer 在 `modelRenderers.js` 中，遵循以下约定：

### 3.1 HiDPI 适配
```js
const dpr = window.devicePixelRatio || 1
ctx.scale(dpr, dpr)
```

### 3.2 颜色主题感知
```js
const isDark = theme === 'dark'
const textColor = isDark ? '#e0e0e0' : '#333'
const bgColor = isDark ? '#1a1a2e' : '#fff'
```

### 3.3 绘制层级
1. 背景 / 水面 / 地面
2. 光线 / 物体
3. 标注 / 标签
4. 刻度 / 辅助线

### 3.4 常见模式
- 光线透明度递增/递减效果：`globalAlpha`
- 虚像用虚线：`ctx.setLineDash([5, 5])`
- 光源发光效果：外层 glow + 内层 core
- 角度标注：`ctx.arc()` + 文字

---

## 四、代码风格

- ES modules（`export default`）
- 无 TypeScript
- 注释用中文
- 变量命名用驼峰（英文）
- 渲染相关函数在 `modelRenderers.js`，物理逻辑在 `modelPhysics.js`

---

## 五、Git 工作流

- 主分支：`main`
- 当前工作分支：`pure-frontend`
- 无后端依赖，所有数据在 constants 目录
